const mongoose = require("mongoose");
const mongoURI = "enter mongo uri";

const connectToMongo = () => {
  mongoose.connect(mongoURI, () => {
    console.log("Connected to Mongo Successfully");
  });
};

module.exports = connectToMongo;
